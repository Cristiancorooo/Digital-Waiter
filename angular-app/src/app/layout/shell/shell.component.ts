import {Component,computed} from '@angular/core';import {RouterLink,RouterLinkActive,RouterOutlet} from '@angular/router';import {AuthService} from '../../core/services/auth.service';import {StoreService} from '../../core/services/store.service';
@Component({selector:'app-shell',standalone:true,imports:[RouterOutlet,RouterLink,RouterLinkActive],template:`<div class="app-shell"><aside><div class="brand"><span>⌁</span><b>DIGITAL <i>WAITER</i></b></div><div class="profile"><div>{{initials()}}</div><span><b>{{store.session()?.name}}</b><small>{{store.session()?.role}}</small></span></div><nav>@for(n of visibleNav();track n.path){<a [routerLink]="n.path" routerLinkActive="active"><span>{{n.icon}}</span>{{n.label}} @if(n.count){<i>{{n.count()}}</i>}</a>}</nav><button class="logout" (click)="auth.logout()">↪ Cerrar sesión</button></aside><main><header><div><h1>Digital Waiter</h1><p>Gestión inteligente para restaurantes</p></div><time>{{today}}</time></header><section class="page"><router-outlet/></section></main></div>`})
export class ShellComponent{today=new Date().toLocaleDateString('es-EC',{weekday:'long',day:'numeric',month:'long'});nav=[{path:'/inicio',label:'Inicio',icon:'▦',roles:['Administrador','Mesero','Cajero']},{path:'/pedidos',label:'Pedidos',icon:'▤',roles:['Administrador','Mesero','Cajero'],count:()=>this.store.orders().filter(o=>o.status!=='paid').length},{path:'/mesas',label:'Mesas',icon:'▱',roles:['Administrador','Mesero','Cajero'],count:()=>this.store.tables().length},{path:'/cocina',label:'Cocina',icon:'◴',roles:['Administrador','Cocina'],count:()=>this.store.orders().filter(o=>['new','preparing','ready'].includes(o.status)).length},{path:'/menu',label:'Menú',icon:'☰',roles:['Administrador','Mesero','Cocina']},{path:'/inventario',label:'Inventario',icon:'◇',roles:['Administrador'],count:()=>this.store.inventory().filter(i=>i.stock<=i.min).length},{path:'/reportes',label:'Reportes',icon:'⌁',roles:['Administrador','Cajero']}];visibleNav=computed(()=>this.nav.filter(n=>n.roles.includes(this.store.session()?.role||'')));initials=computed(()=>this.store.session()?.name.split(' ').map(x=>x[0]).slice(0,2).join('').toUpperCase());constructor(public store:StoreService,public auth:AuthService){}}
