import { Injectable, NotFoundException, OnModuleInit } from '@nestjs/common'; import { InjectRepository } from '@nestjs/typeorm'; import { Repository } from 'typeorm'; import { EstadoMesa, Mesa } from '../database/entities';
@Injectable() export class MesasService implements OnModuleInit { constructor(@InjectRepository(Mesa) private repo:Repository<Mesa>){} async onModuleInit(){if(await this.repo.count())return;await this.repo.save(Array.from({length:12},(_,i)=>this.repo.create({numero:i+1,capacidad:i%3===0?6:4})))} all(){return this.repo.find({order:{numero:'ASC'}})} create(dto:Partial<Mesa>){return this.repo.save(this.repo.create(dto))} async status(id:number,estado:string){const mesa=await this.repo.findOneBy({id});if(!mesa)throw new NotFoundException('Mesa no encontrada');mesa.estado=estado as EstadoMesa;return this.repo.save(mesa)} }
