import {ConflictException,Injectable,NotFoundException} from '@nestjs/common';import {InjectRepository} from '@nestjs/typeorm';import {hash} from 'bcryptjs';import {Repository} from 'typeorm';import {Usuario} from '../database/entities';
@Injectable() export class UsuariosService{constructor(@InjectRepository(Usuario) private repo:Repository<Usuario>){}async all(restauranteId:number){return(await this.repo.find({where:{restauranteId},order:{nombre:'ASC'}})).map(({passwordHash,...user})=>user)}async create(restauranteId:number,dto:{usuario:string;nombre:string;password:string;rol:string}){const usuario=dto.usuario.trim().toLowerCase();if(await this.repo.existsBy({usuario}))throw new ConflictException('El nombre de usuario ya existe');const user=await this.repo.save(this.repo.create({usuario,nombre:dto.nombre.trim(),rol:dto.rol,passwordHash:await hash(dto.password,10),restauranteId}));const{passwordHash,...safe}=user;return safe}async active(restauranteId:number,id:number,activo:boolean){const user=await this.repo.findOneBy({id,restauranteId});if(!user)throw new NotFoundException('Usuario no encontrado');user.activo=activo;await this.repo.save(user);const{passwordHash,...safe}=user;return safe}async password(restauranteId:number,id:number,password:string){const user=await this.repo.findOneBy({id,restauranteId});if(!user)throw new NotFoundException('Usuario no encontrado');user.passwordHash=await hash(password,10);await this.repo.save(user);return{message:'Contraseña actualizada'}}}
