import {Component,inject,signal} from '@angular/core';import {DecimalPipe} from '@angular/common';import {FormBuilder,ReactiveFormsModule,Validators} from '@angular/forms';import {StoreService} from '../../core/services/store.service';import {InventoryItem} from '../../models/app.models';
@Component({selector:'app-inventario',standalone:true,imports:[DecimalPipe,ReactiveFormsModule],template:`<div class="page-title"><div><h2>Inventario</h2><p>Existencias, movimientos y alertas</p></div><button class="primary" (click)="edit()">+ Registrar producto</button></div><section class="panel inventory"><div class="table-head-row"><b>Producto</b><b>Categoría</b><b>Stock</b><b>Mínimo</b><b>Acciones</b></div>@for(i of store.inventory();track i.id){<div class="table-data-row" [class.low]="i.stock<=i.min"><b>{{i.name}}</b><span>{{i.category}}</span><strong>{{i.stock|number:'1.0-2'}} {{i.unit}}</strong><span>{{i.min|number:'1.0-2'}} {{i.unit}}</span><div><button class="small-button" (click)="store.moveStock(i.id,1)">+ Entrada</button><button class="small-button" (click)="store.moveStock(i.id,-1)">− Salida</button><button class="small-button" (click)="edit(i)">Editar</button></div></div>}</section>@if(editing()){<div class="dialog"><form [formGroup]="form" (ngSubmit)="save()"><button type="button" class="dialog-close" (click)="editing.set(false)">×</button><small>INVENTARIO</small><h2>{{form.value.id?'Editar':'Registrar'}} producto</h2><label>Producto<input formControlName="name"></label><label>Categoría<input formControlName="category"></label><label>Unidad<select formControlName="unit"><option>kg</option><option>L</option><option>u</option></select></label><label>Stock<input type="number" formControlName="stock"></label><label>Mínimo<input type="number" formControlName="min"></label><button class="primary" type="submit">Guardar</button></form></div>}`})
export class InventarioComponent{private fb=inject(FormBuilder);public store=inject(StoreService);editing=signal(false);form=this.fb.nonNullable.group({id:[0],name:['',Validators.required],category:['',Validators.required],unit:['kg'],stock:[0,Validators.min(0)],min:[0,Validators.min(0)]});edit(i?:InventoryItem){this.form.reset(i?{...i}:{id:0,name:'',category:'',unit:'kg',stock:0,min:0});this.editing.set(true)}async save(){if(this.form.invalid)return;try{await this.store.saveInventory(this.form.getRawValue());this.editing.set(false)}catch{}}}

