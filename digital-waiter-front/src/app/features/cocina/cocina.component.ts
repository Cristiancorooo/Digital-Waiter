import {Component} from '@angular/core';import {StoreService} from '../../core/services/store.service';import {Order,OrderStatus} from '../../models/app.models';
@Component({selector:'app-cocina',standalone:true,template:`<div class="page-title"><div><h2>Monitor de cocina</h2><p>Actualiza el estado de cada preparación</p></div></div><div class="kanban">@for(group of groups;track group.status){<section><header><b>{{group.label}}</b><span>{{orders(group.status).length}}</span></header>@for(o of orders(group.status);track o.id){<article class="ticket"><div><h3>{{store.place(o)}}</h3><small>Pedido #{{o.id}} · {{o.customer||o.waiter||'Cliente'}}</small></div><ul>@for(line of o.items;track line.menuId){<li>{{line.qty}}× {{name(line.menuId)}}</li>}</ul>@if(o.status!=='ready'){<button (click)="advance(o)">{{action(o.status)}} →</button>}@else{<p class="handoff-state">✓ {{o.modality==='pickup'?'Cliente notificado para retirar':'Listo para retirar por el mesero'}}</p>}</article>}@empty{<p class="empty">Sin pedidos</p>}</section>}</div>`})
export class CocinaComponent{groups=[{status:'new' as OrderStatus,label:'NUEVOS'},{status:'preparing' as OrderStatus,label:'EN PREPARACIÓN'},{status:'ready' as OrderStatus,label:'LISTOS PARA RETIRAR'}];constructor(public store:StoreService){}orders(s:OrderStatus){return this.store.orders().filter(o=>o.status===s)}name(id:number){return this.store.menu().find(m=>m.id===id)?.name}action(s:OrderStatus){return s==='new'?'Empezar':'Marcar listo'}advance(o:Order){if(o.status!=='ready')this.store.setStatus(o.id,o.status==='new'?'preparing':'ready')}}

