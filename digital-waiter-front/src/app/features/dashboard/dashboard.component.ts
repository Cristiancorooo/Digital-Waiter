import {Component,computed} from '@angular/core';import {CurrencyPipe} from '@angular/common';import {StoreService} from '../../core/services/store.service';
@Component({selector:'app-dashboard',standalone:true,imports:[CurrencyPipe],template:`<div class="page-title"><div><h2>Panel de control</h2><p>Resumen actualizado de la operación</p></div></div><div class="stats"><article><span>💰</span><b>{{sales()|currency:'USD'}}</b><small>VENTAS DEL DÍA</small></article><article><span>🧾</span><b>{{activeOrders()}}</b><small>PEDIDOS ACTIVOS</small></article><article><span>🪑</span><b>{{occupied()}} / {{store.tables().length}}</b><small>MESAS OCUPADAS</small></article><article><span>📦</span><b>{{lowStock()}}</b><small>ALERTAS DE STOCK</small></article></div><div class="two-cols"><section class="panel"><h3>Estado operativo</h3><div class="bars"><i style="height:45%"></i><i style="height:62%"></i><i style="height:52%"></i><i style="height:85%"></i><i style="height:72%"></i><i style="height:92%"></i></div></section><section class="panel"><h3>Pedidos recientes</h3>@for(o of recent();track o.id){<div class="row"><span class="number">{{o.table.toString().padStart(2,'0')}}</span><div><b>Pedido #{{o.id.toString().padStart(3,'0')}}</b><small>{{o.waiter||'Sin asignar'}} · {{o.items.length}} líneas · {{store.total(o)|currency:'USD'}}</small></div><em>{{label(o.status)}}</em></div>}@empty{<p class="empty">Aún no existen pedidos.</p>}</section></div>`})
export class DashboardComponent{sales=computed(()=>this.store.payments().filter(p=>new Date(p.date).toDateString()===new Date().toDateString()).reduce((s,p)=>s+p.total,0));activeOrders=computed(()=>this.store.orders().filter(o=>o.status!=='paid').length);occupied=computed(()=>this.store.tables().filter(t=>t.status!=='available').length);lowStock=computed(()=>this.store.inventory().filter(i=>i.stock<=i.min).length);recent=computed(()=>[...this.store.orders()].reverse().slice(0,4));constructor(public store:StoreService){}label(s:string){return({new:'Nuevo',preparing:'Preparando',ready:'Listo',payment:'Por cobrar',paid:'Pagado'} as Record<string,string>)[s]}}
